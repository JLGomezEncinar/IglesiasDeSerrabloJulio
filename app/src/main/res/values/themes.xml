<?xml version="1.0" encoding="utf-8"?>
<resources>


    <style name="Theme.IglesiasDeSerrablo" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Color de fondo -->
        <item name="windowSplashScreenBackground">#F3E9D2</item>

        <!-- Logo -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/logo</item>

        <!-- Tema real de la app -->
        <item name="postSplashScreenTheme">@style/Theme.IglesiasDeSerrablo</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
</resources>